[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "spreadsheet-cells"
version = "0.1.0"
description = "Spreadsheet cell architecture with oscillator/RNG for multi-agent fleet coordination"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/SuperInstance/spreadsheet-cells"
